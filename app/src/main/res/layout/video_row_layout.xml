<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              tools:context="aimscapstone.infectionpreventionsource.VideoNavigationActivity"
              android:orientation="vertical"
              android:padding="20dp">
    <ImageView
        android:id="@+id/videoImageView"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:scaleType="centerCrop"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:weightSum="1"
        android:background="@color/primary_color">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/videoRowTitle"
            android:text="Proper Glove Technique"
            android:layout_weight="0.5"
            android:textStyle="bold"
            android:textColor="#ffffff"
            android:padding="10dp"/>
        <TextView
            android:id="@+id/videoRowDesc"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Learn proper glove usage with Johns Hopkins, Dr. Carr."
            android:textColor="#ffffff"
            android:layout_weight="0.5"
            android:padding="10dp"/>
    </LinearLayout>

</LinearLayout>